# ─── Render slug exclusions (Heroku/Render buildpack-based deploys) ───────────
# Mirrors .dockerignore — keeps the deployed slug lean.
# NOTE: Do NOT add *.pkl here — the prediction endpoint may depend on model files.

venv/
.git/
.github/
tests/
docs/
*.md
*.pyc
__pycache__/
local_test_images/
sample_data.csv
